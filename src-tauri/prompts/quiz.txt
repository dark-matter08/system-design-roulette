You are writing a quiz to test understanding (not recall) of a system design course the reader studied yesterday.

Course markdown follows between the markers.
---COURSE START---
{{COURSE}}
---COURSE END---

Write exactly 5 questions: 3 multiple-choice and 2 free-text.
- MCQ: 4 plausible choices, exactly one correct. Distractors must reflect real misconceptions.
- Free-text: scenario-based ("you are designing X, what happens when Y"), answerable in 2-4 sentences.
- Each question needs an explanation that teaches, not just states the answer.
- correct_answer for MCQ is the exact text of the correct choice. For free-text it is a model answer in 2-4 sentences.

Your entire final reply must be exactly one fenced JSON code block (```json ... ```) matching this schema, with no text before or after it:
{
  "questions": [
    { "prompt": string, "kind": "mcq"|"free", "choices": [string,string,string,string] | null, "correct_answer": string, "explanation": string }
  ]
}
